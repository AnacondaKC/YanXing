'use client'

// The original page entry delegates data orchestration to the native controller.
// Original presentation components are reused there; retired write flows stay disconnected.
export { default } from '@/components/workspace-app'
